{
  "name": "node-modules-remover",
  "version": "1.0.0",
  "main": "main.js",
  "license": "MIT",
  "dependencies": {
    "electron": "^2.0.5",
    "electron-reload": "^1.2.5",
    "styled-components": "^3.3.3",
    "trash": "^4.3.0"
  },
  "devDependencies": {
    "concurrently": "^3.6.0",
    "wait-on": "^2.1.0"
  },
  "scripts": {
    "react-start": "cd src && yarn start",
    "electron-start": "electron .",
    "start": "concurrently 'yarn run react-start' 'wait-on http://localhost:3000 && yarn run electron-start'"
  }
}
